{
  "name": "cyrano-sniffer",
  "version": "0.1.0",
  "description": "Passive Cyrano (EFP1.1) UDP sniffer that publishes OPP2 to an MQTT broker",
  "main": "src/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "start": "node src/index.js",
    "replay": "node src/index.js --replay",
    "test": "node --test"
  },
  "dependencies": {
    "mqtt": "^5.0.0"
  },
  "optionalDependencies": {
    "cap": "^0.2.1"
  }
}
